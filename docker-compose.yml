version: '2'
services:
  mongo:
    image: mongo
  node:
    build: docker/node
    user: node
    working_dir: /home/node/app
    volumes:
      - ./app:/home/node/app
    ports:
      - 1337:8080
    command: nodemon -L index.js
  vue:
    build: front
    volumes:
      - ./front:/app
    ports:
      - 8001:8081
    links:
      - node
